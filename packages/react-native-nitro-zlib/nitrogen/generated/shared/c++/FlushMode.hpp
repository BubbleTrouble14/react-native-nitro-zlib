///
/// FlushMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::rnzlib {

  /**
   * An enum which can be represented as a JavaScript union (FlushMode).
   */
  enum class FlushMode {
    NO_FLUSH      SWIFT_NAME(noFlush) = 0,
    PARTIAL_FLUSH      SWIFT_NAME(partialFlush) = 1,
    SYNC_FLUSH      SWIFT_NAME(syncFlush) = 2,
    FULL_FLUSH      SWIFT_NAME(fullFlush) = 3,
    FINISH      SWIFT_NAME(finish) = 4,
    BLOCK      SWIFT_NAME(block) = 5,
    TREES      SWIFT_NAME(trees) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::rnzlib

namespace margelo::nitro {

  using namespace margelo::nitro::rnzlib;

  // C++ FlushMode <> JS FlushMode (union)
  template <>
  struct JSIConverter<FlushMode> {
    static inline FlushMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("NO_FLUSH"): return FlushMode::NO_FLUSH;
        case hashString("PARTIAL_FLUSH"): return FlushMode::PARTIAL_FLUSH;
        case hashString("SYNC_FLUSH"): return FlushMode::SYNC_FLUSH;
        case hashString("FULL_FLUSH"): return FlushMode::FULL_FLUSH;
        case hashString("FINISH"): return FlushMode::FINISH;
        case hashString("BLOCK"): return FlushMode::BLOCK;
        case hashString("TREES"): return FlushMode::TREES;
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert \"" + unionValue + "\" to enum FlushMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, FlushMode arg) {
      switch (arg) {
        case FlushMode::NO_FLUSH: return JSIConverter<std::string>::toJSI(runtime, "NO_FLUSH");
        case FlushMode::PARTIAL_FLUSH: return JSIConverter<std::string>::toJSI(runtime, "PARTIAL_FLUSH");
        case FlushMode::SYNC_FLUSH: return JSIConverter<std::string>::toJSI(runtime, "SYNC_FLUSH");
        case FlushMode::FULL_FLUSH: return JSIConverter<std::string>::toJSI(runtime, "FULL_FLUSH");
        case FlushMode::FINISH: return JSIConverter<std::string>::toJSI(runtime, "FINISH");
        case FlushMode::BLOCK: return JSIConverter<std::string>::toJSI(runtime, "BLOCK");
        case FlushMode::TREES: return JSIConverter<std::string>::toJSI(runtime, "TREES");
        default: [[unlikely]]
          throw std::runtime_error("Cannot convert FlushMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("NO_FLUSH"):
        case hashString("PARTIAL_FLUSH"):
        case hashString("SYNC_FLUSH"):
        case hashString("FULL_FLUSH"):
        case hashString("FINISH"):
        case hashString("BLOCK"):
        case hashString("TREES"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
